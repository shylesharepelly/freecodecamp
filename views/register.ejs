<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <%- include('navbar.ejs',{loggedin:false}) %>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <h6 class="py-4">Signup as a new user</h6>
            <div>
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                            <li> <%= messages.error[i] %></li>
                    <% } %>
                  <% } %>
            </div>
            <form action="/users" method="post">
                <div class="py-3">
                    <label for="firstName">User Name:</label>
                    <input type="text" name="name" class="border border-gray-300 text-black-300 rounded p-2 w-full">
                </div>
               
                <div class="py-3">
                    <label for="email">Email:</label>
                    <input type="email" name="email" class="border border-gray-300 text-black-300 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="password">Password:</label>
                    <input type="password" name="password" class="border border-gray-300 text-black-300 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Sign up</button>
                </div>
            </form>
            <div class="flex">
                <h1>Already have an account</h1> &nbsp;
                <a href="/login" class="underline text-blue-900">Sign In</a>
            </div>
            <div class="flex">
               
                <a href="/google" class="underline text-blue-900">Sign In with Google</a>
            </div>
        </div>
    </div>
</body>
</html>